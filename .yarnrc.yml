# node-modules instead of PnP: Vite, Playwright and MSW work without patches or editor SDKs
nodeLinker: node-modules

# Public registry: the starter must install without a corporate VPN
npmRegistryServer: 'https://registry.npmjs.org'

enableGlobalCache: true
compressionLevel: mixed
